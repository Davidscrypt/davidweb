[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# Make the form endpoint available at /api/lead on Netlify
[[redirects]]
  from = "/api/lead"
  to = "/.netlify/functions/lead"
  status = 200
